import { css } from "@emotion/react";

const override = css`
  display: block;
  margin-top: 80px;
  margin-left: 50%;
  border-color: red;
`;

const LoaderCss = () => {
  return override;
};

export default LoaderCss;
